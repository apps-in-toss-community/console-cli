---
'@ait-co/console-cli': patch
---

chore(deps): bump vitest to 4.1.5 (patch); isolate XDG_CONFIG_HOME in credentials env-source tests so a developer's real `~/.config/aitcc/auth-state.json` doesn't leak into the suite
